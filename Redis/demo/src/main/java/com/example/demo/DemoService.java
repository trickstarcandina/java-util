package com.example.demo;

import org.springframework.stereotype.Service;

@Service
public class DemoService {
    Object forwardRequest(String token, String pathName, Object objectRequest) {
        //demo nên ko viết tường minh (so don't write it explicitly)
        return null;
    }
}
